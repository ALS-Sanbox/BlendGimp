schema_version = "1.0.0"

id = "blendgimp"
version = "0.1.0"
name = "BlendGimp"
tagline = "GIMP-powered texture painting for Blender"
maintainer = "Afro Lion Studios"
type = "add-on"

blender_version_min = "5.0.0"

license = [
    "SPDX:GPL-3.0-or-later",
]

[permissions]
network = "Connect to the local BlendGimp GIMP service"
files = "Detect and launch the user's GIMP executable"